[project]
name = "flexsearch"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "numpy",
    "networkx",
]

[project.optional-dependencies]
embed = ["onnxruntime", "transformers"]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.backends._legacy:_Backend"
