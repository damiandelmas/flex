"""instant — no-embed projection + code graph cell (promoted from labs).

Compiles folder selections into one FTS5/SQL cell with a recursive node tree,
call/import graph (Python ast + JS/TS tree-sitter), and SOMA file_uuid hinge.
Watchable via refresh.py (size-signature, regen on change).
"""
