---
name: flex:sessions:codex
description: Compatibility alias for Codex session history. Use only when an older prompt explicitly names flex:sessions:codex; route turns, tool calls, files, delegation, source recovery, and semantic history search through flex:sessions with provider=codex.
allowed-tools:
  - mcp__flex__flex
---

# flex:sessions:codex Compatibility

This is a compatibility alias, not a canonical Flex door.

Continue with `$flex:sessions`, selecting `cell="codex"`. Run `@orient` before
the first query, then follow the shared coding-agent session contract.
