interface:
  display_name: "Codex Sessions (Compatibility)"
  short_description: "Forward Codex history to flex:sessions"
  default_prompt: "Use $flex:sessions:codex as a compatibility alias and continue through $flex:sessions with cell=codex."
policy:
  allow_implicit_invocation: false
