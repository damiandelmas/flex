---
name: flex:hn
description: Compatibility alias for searching an installed Hacker News corpus cell. Use only when an older prompt explicitly names flex:hn; route stories, comments, rankings, thread opinion, and semantic HN search through the generic flex interface with cell=hn.
allowed-tools:
  - mcp__flex__flex
---

# flex:hn Compatibility

This is a compatibility alias, not a canonical Flex door.

Continue with `$flex`, selecting `cell="hn"`. Run `@orient` before the first
query, then follow the cell's bundled schema, presets, views, fidelity notes,
and source limits. Do not duplicate its corpus-retrieval manual here.
