---
name: flex:sessions:claudecode
description: Compatibility alias for Claude Code session history. Use only when an older prompt explicitly names flex:sessions:claudecode; route turns, tool calls, files, delegation, source recovery, and semantic history search through flex:sessions with provider=claude_code.
allowed-tools:
  - mcp__flex__flex
---

# flex:sessions:claudecode Compatibility

This is a compatibility alias, not a canonical Flex door.

Continue with `$flex:sessions`, selecting `cell="claude_code"`. Run `@orient`
before the first query, then follow the shared coding-agent session contract.
