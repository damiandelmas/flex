interface:
  display_name: "Claude Code Sessions (Compatibility)"
  short_description: "Forward Claude Code history to flex:sessions"
  default_prompt: "Use $flex:sessions:claudecode as a compatibility alias and continue through $flex:sessions with cell=claude_code."
policy:
  allow_implicit_invocation: false
